import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  paper: {
    border: "1px solid",
    padding: 10,
    backgroundColor: "#FFF",
  },
  bg: {
    backgroundColor: "#FFF",
  },
}));
